## Build instructions (tested on Linux)

1. Install PyInstaller if not present yet: `pip install pyinstaller`
2. Run `pyinstaller pytrim.spec`
3. The generated standalone `pytrim` binary will be located under `dist/`