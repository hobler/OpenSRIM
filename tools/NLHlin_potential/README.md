This directory contains the script `fit_screens.py` that produces a table of parameters for the NLHlin potential using screening functions obtained with DMol (included in the `dmol_repulsive_screen subdirectory`). By setting the flags in the code, it may also be used to compare the fit to the DMol or MP2 data.

The script was originally named `fit_screens_lin.py`.
